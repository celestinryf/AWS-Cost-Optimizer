name: Release

# Triggered when you push a version tag, e.g.:
#   git tag v0.2.0 && git push origin v0.2.0
on:
  push:
    tags:
      - "v[0-9]+.[0-9]+.[0-9]+"

jobs:
  build-release:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-latest
            target: aarch64-apple-darwin
            suffix: ""
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            suffix: ".exe"
          - os: ubuntu-22.04
            target: x86_64-unknown-linux-gnu
            suffix: ""

    runs-on: ${{ matrix.os }}
    name: Release (${{ matrix.os }})

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # ---- Rust ----
      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Cache Rust build
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: client/src-tauri -> target

      # ---- Python / PyInstaller ----
      - name: Set up Python 3.13
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"
          cache: pip
          cache-dependency-path: server/requirements-bundle.txt

      - name: Install Python dependencies
        run: pip install -r server/requirements-bundle.txt

      # Linux: system libraries required by Tauri
      - name: Install Linux system deps
        if: matrix.os == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libssl-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev

      - name: Build Python sidecar (PyInstaller)
        working-directory: server
        run: pyinstaller aws-cost-optimizer-api.spec

      - name: Copy sidecar to Tauri binaries dir
        shell: bash
        run: |
          mkdir -p client/src-tauri/binaries
          SRC="server/dist/aws-cost-optimizer-api${{ matrix.suffix }}"
          DST="client/src-tauri/binaries/aws-cost-optimizer-api-${{ matrix.target }}${{ matrix.suffix }}"
          cp "$SRC" "$DST"

      # ---- Node / Tauri ----
      - name: Set up Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: client/package-lock.json

      - name: Install frontend dependencies
        working-directory: client
        run: npm ci

      - name: Generate Tauri icons
        shell: bash
        working-directory: client
        run: |
          mkdir -p src-tauri/icons
          python3 << 'PYEOF'
          import struct, zlib
          def chunk(tag, data):
              crc = zlib.crc32(tag + data) & 0xffffffff
              return struct.pack('>I', len(data)) + tag + data + struct.pack('>I', crc)
          n = 1024
          rows = b''.join(b'\x00' + bytes([37, 99, 235]) * n for _ in range(n))
          png = (
              b'\x89PNG\r\n\x1a\n'
              + chunk(b'IHDR', struct.pack('>IIBBBBB', n, n, 8, 2, 0, 0, 0))
              + chunk(b'IDAT', zlib.compress(rows, 9))
              + chunk(b'IEND', b'')
          )
          open('icon-src.png', 'wb').write(png)
          PYEOF
          npm run tauri -- icon icon-src.png
          rm -f icon-src.png

      # ---- Inject signing pubkey ----
      # tauri.conf.json ships with pubkey="" so that desktop CI builds (which
      # don't have the signing secret) can compile without signing. For releases
      # we stamp the real pubkey in here so tauri-action embeds it in the binary
      # and generates a correctly-signed latest.json.
      - name: Stamp updater pubkey into tauri.conf.json
        shell: python3 {0}
        run: |
          import json, pathlib
          PUBKEY = "dW50cnVzdGVkIGNvbW1lbnQ6IG1pbmlzaWduIHB1YmxpYyBrZXk6IEM5MUZERDIxNkNBMzkwNTEKUldSUmtLTnNJZDBmeWZZUTJBTlFiQjFLSVlEOEhSUi9hUyt3L3dSWFJNaFpqdS8rTVlSSnROLzUK"
          p = pathlib.Path("client/src-tauri/tauri.conf.json")
          cfg = json.loads(p.read_text())
          cfg.setdefault("plugins", {}).setdefault("updater", {})["pubkey"] = PUBKEY
          p.write_text(json.dumps(cfg, indent=2) + "\n")
          print("Pubkey stamped.")

      # ---- Build + Publish release ----
      # tauri-action creates the GitHub Release (draft=false), attaches all
      # platform installers, and generates latest.json for the auto-updater.
      #
      # Required secrets:
      #   TAURI_SIGNING_PRIVATE_KEY        — from: tauri signer generate
      #   TAURI_SIGNING_PRIVATE_KEY_PASSWORD — passphrase you chose (empty if none)
      #
      # Optional (macOS notarization):
      #   APPLE_CERTIFICATE, APPLE_CERTIFICATE_PASSWORD
      #   APPLE_SIGNING_IDENTITY, APPLE_ID, APPLE_PASSWORD, APPLE_TEAM_ID
      - name: Build Tauri app and publish to GitHub Releases
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
        with:
          projectPath: client
          tagName: ${{ github.ref_name }}
          releaseName: "AWS Cost Optimizer ${{ github.ref_name }}"
          releaseBody: |
            Download the installer for your platform below.

            **macOS (Apple Silicon):** `.dmg`
            **Windows:** `.msi` or `.exe` (NSIS)
            **Linux:** `.AppImage` or `.deb`

            The app will prompt you to install future updates automatically.
          releaseDraft: false
          prerelease: false
          args: --target ${{ matrix.target }}
