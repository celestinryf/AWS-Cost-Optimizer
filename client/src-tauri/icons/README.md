# Icons

Place your app icons here before running `tauri build`.

Required files:
- `32x32.png`
- `128x128.png`
- `128x128@2x.png`
- `icon.icns` (macOS)
- `icon.ico` (Windows)

Generate from a single 1024x1024 source PNG using the Tauri CLI:
```bash
npm run tauri icon path/to/icon-1024.png
```
